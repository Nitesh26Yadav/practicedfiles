import csv
import pandas as pd

df=pd.read_csv('DeptTest.csv')

print(df)
print(df.head())
print(df.tail())
print(df['Name'].value_counts)
print(df.loc[0:10])
print(df.shape)
print(df.columns)
# result = result1.drop(labels='initial_token',axis=1)axis 0 means checks in rows axis =1 means checks in columns
