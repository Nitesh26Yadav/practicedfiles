import boto3
import os.path

def upload_file_using_client():
    s3 = boto3.client("s3")

    bucket_name = "beginning.00"
    object_name = "sample.txt"
    file_name = os.path.join(r"C:\Users\rsa-l\Desktop\vs py code\aws.py\outputrsa.1")


    response = s3.upload_file(file_name,bucket_name,object_name)
    

    if response == None:
        print("file uploaded")
    else:
        print("file is not uploaded")

upload_file_using_client()

